machine:
 services:
   - docker

dependencies:
 override:
   - docker info
   - docker build --rm=false -t lukysekbay/pokus .

test:
 post:
   - docker run -d -p 8080:8080 lukysekbay/pokus; sleep 10
   - curl --retry 10 --retry-delay 5 localhost:8080 | grep "Hello world"

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
